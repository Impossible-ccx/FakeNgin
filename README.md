# FakeNgin

## Setup

python 3.9
需要的库已记录在requirements.txt中。对他们使用conda install应该就能获取全部依赖

## Run

```bash
python src/app.py
```

网页开放于 http://127.0.0.1:5000/ 

## Model

本地模型使用ollama接入。添加其他模型查看src/checkmodel/下的__init__.py和base.py
